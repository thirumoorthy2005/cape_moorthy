version: 0.2
phases:
  pre_build:
    commands:
    - echo "Entered the pre-build phase"
  build:
    commands:
     - echo "Entered build phase"
     - mvn clean install
artifacts:
  files:
    - target/LV-Safety-Verification-0.0.1-SNAPSHOT.jar